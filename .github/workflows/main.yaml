name: amii-lab3-workflow
run-name: ${{ github.actor }} is  deploying a new model
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy model
        run: ./.github/workflows/deploy.sh
        shell: bash